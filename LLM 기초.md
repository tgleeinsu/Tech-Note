# LLM 기초

## LLM
- Lagre Language Model == AI엔진 -> AI엔진을 쓴 제품이 Claude, Chat GPT 등등
    - 다만, AI 자체는 광범위한 영역이고 LLM은 AI영역중 텍스트를 다루는 종류.
    - 예를 들어 이미지, 음성까지 처리하는 모델은 멀티모달 모델이라고 불림.
- Claude, Chat GPT, Gemini는 LLM을 제공하는 서비스의 이름.
- Opus 4.8, GPT-5, Gemini Pro 등이 LLM
- claude.ai, ChatGPT, Gemini 앱등이 LLM모델을 쓰기 쉽게 인터페이스화 한 제품(툴)
- LLM(엔진) 기반으로 채팅앱도 만들고, API로도 쓰고, Idea CLI에도 박아서 사용한 다는 것을 이해할 수 있어야함

## LLM 레벨 핵심용어 
1. token 
    - 입/출력의 최소 단위
        - 글자나 단어 단위라고 딱 잘라서 말하기는 어렵고, subworld 단위로 잘라서 처리. 
        ex) apple -> 1 토큰, antidisestabiishment -> 여러 토큰 
    - LLM 사용 비용, 컨텍스트의 단위
        - 입력, 출력 각각 단가가 붙는다
2. autogressive
    - 모델은 자기회귀를 통해서 최종 출력값을 내놓는다.
        - 답을 한번에 내놓는게 아니라 토큰 하나를 소비해 단어 하나를 예측해 쓰고 -> 방금 쓴걸 포함해 지금까지 쓴 내용을 다시 보고 -> 다시 다음 단어 하나를 예측해서 쓴다 -> 반복
        - 100토큰짜리 답이면 이 과정을 100번 돈다고 볼 수 있음
    - 주어진 입력값을 읽는 것은 빠름.
        - 출력. 답을 내는 것은 느림. 왜냐하면 답은 아직 존재하지 않고, 한 단어를 만들어야 다음 단어를 만들 재료가 생기기 때문에 병렬 처리가 안됨. 순차처리로 자기회귀를 하면서 답변을 만들어내야함
    - 질문이 길어서 느린경우는 없음. 출력을 많이 해야할때 느려짐
3. context window
    - 대화 전체의 공간 단위. 길어질수록 토큰 소비량 비례
    - 기본적으로 LLM은 특정 메모리에 입력하지 않는 이상 이전 대화에 대한 기억을 가지고 있지 않는다. 대화를 할때마다 이전 대화 전체를 다시 입력으로 밀어 넣고 출력을 하는 것임.
    - context는 맥락이고, window는 맥락이 들어갈 수 있는 최대 공간을 의미
        - token 단위 (ex: 1만 토큰)
    - 대화가 길어질수록 매 턴마다 누적된 전체 대화를 입력 토큰으로 계산하니 대화가 길어질수록 토큰 사용량이 비대해짐.
    그리고 한도를 넘으면 앞부분을 잘라서 모델이 초반 내용을 잊게됨
4. hallucination
    - 모델은 기본적으로 항상 참을 출력하지 않음
        - 매번 데이터베이스를 조회하거나 매번 사실 검증을 통하고 출력을 하면 이럴 일이 없지만 기본적으로 가장 그럴듯한 단어, 문장을 이어붙히도록 설계 되어있음
    - 즉, 입력된 내용에 대한 답을 찾아내는게 아니라 추론하도록 되어있는데, 그렇기 때문에 거짓을 말할때도 있음
        - LLM의 출력은 초안으로써 의미가 있는 것이지 검증된 사실이 아니란 것을 인지해야함
    - 추론 방식은 그대로 두고, 거짓을 지어내는 행동을 줄일 수 있는 방법은 보통 검색 연동, 출처 강제, 검증 단계 추가등이 있음
5. tempaerature
    - 매 토큰마다 모델은 출력값에 대한 확률 분포를 내놓음. 그 분포에서 어떤 토큰을 고를지에 대한 값임
        - 예를들어 temperature = 0 이라면 항상 가장 확률 높은 토큰만 골라서 거의 같은 입력 -> 같은 출력의 답변만 내놓게 되고, 값을 높히면 분포가 평평해져서 정확도가 낮더라도 다양성, 창의성 있는 토큰을 고르게 됨.
        - 코드, 추출, 분류처럼 정답이 있는 작업은 temperature를 낮게, 브레인스토밍, 창작은 높게 쓰는게 일반적
6. usage
    - 토큰 계량기. 입력에 사용된 토큰 양과 출력에 사용된 토큰 양을 볼 수 있음

LLM레벨에서 모든 요소를 꿰뚫는 하나의 핵심 요소는 토큰임.
비용도 토큰, 속도도 토큰, 컨텍스트도 토큰, 정확도/창의성도 토큰으로 귀결됨.

## LLM 호출 파라미터
claude 기준 - 