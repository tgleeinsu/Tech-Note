# 자료형 변환
- 32비트 정수 int를 32비트 부동 소수점으로 치환할때 정밀도 손실(오차) 있음.
    - 32비트 부동 소수점의 가수(24비트)로는 32비트를 표현 불가
- 부동소수점은 근사치로 실수를 표현
    - 부동 소수점끼리 비교할때 동치 연산자 == 사용하면 안됨. 0.1이 부동소수점 내부에서는 0.1가 아닐 수 있음
    - epsilon을 비교하는 방식으로 할 수 있음. 
        ```
        // num1 - num2를 빼고 절대값을 구한게 정말정말 작은수와 비교

        if (Math.Abs(num1 - num2) <= Single.Epsilon) {

        }
        ```

- 32비트 정수 -> 64비트 부동 소수점으로 치환은 정밀도 손실이 없음. 가수 표현 범위가 32비트 정수를 충분히 커버하고도 남기 때문
    - 어떤 프로그래밍 언어든 32비트 정수를 64비트 double형으로 대입할때 캐스팅 형변환 없이 혼용을 하는 이유

- 16비트 부동소수점은 32비트에 비해 가수 표현범위가 10비트밖에 안됨. 표현범위도 훨씬 줄어들음. 그 대신 속도 2배 빨라 컴퓨터 그래픽이나 머신러닝에서 종종 사용
    - 색상 표현할 때 사용 (0~255 이므로 8비트)
    - 유효숫자가 3자리로 충분하면 16비트 부동소수점이면 충분

- CPU는 32와 64비트만 지원
    - 계산은 32비트 계산이 더 빠름
    - 크기는 64비트가 두배. CPU캐시를 사용하므로 이로인해 더 느려질 수 있음.
    - 일반적으로 32비트 쓰고 높은 정밀도, 낮은 오차가 필요하면 64비트를 씀.
    - 빠른 속도가 필요하면 16비트 쓰는 경우까지 있음 (GPU)


