# LLM-tool_use

## 1. LLM - tool_use - 도구

- LLM은 직접 도구를 실행하지 않는다. 도구를 호출할 뿐이다. 도구를 실행하는 것은 언제나 도구를 제공하는 외부 서비스, 프로그램이다
- 지라 티켓 만들어줘 -> 지라 티켓을 생성하는 도구를 실행하는 create_issue() 호출
- 도구는 무엇인가?
    - 모델 호출할 수 있도록 정의된 함수
    - 외부 서비스에서 제공하는 기능 함수가 될 수도 있고, 순수 함수가 될수도 있다
        - get_weather(city) — 날씨 API를 호출하는 함수
        - calculator(expression) — 그냥 수식을 계산하는 로컬 함수
        - jira / create_issue(repo, title, body)ß
    - 외부 서비스: 깃헙, 지라, 노션
    - 도구: 외부서비스 API기능들을 사용할 수 있도록 도구로써 제공 (함수)
    - MCP: 개발자가 API문서를 보고 직접 도구 하나하나를 연결하는게 아니라, 서비스쪽에서 표준형식으로 제공하는 서버
        - 서비스에서 제공하는 MCP를 모델에 연결하면 그 안에 정의된 도구들이 자동으로 모델들에 노출되고 모델은 tool_use로 도구를 실행

    ![alt text](<resource/스크린샷 2026-07-07 오후 2.44.00.png>)


## 2. tool_use 흐름


![alt text](<resource/스크린샷 2026-07-07 오후 3.51.22.png>)
![alt text](<resource/스크린샷 2026-07-07 오후 3.54.17.png>)

        
```
전제조건: LLM은 텍스트 기반 모델이다. 그렇기 때문에 실제 행동을 할수는 없다.
```

0. 도구 목록을 LLM에게 전달해 세팅
    - 개발자가 혹은 MCP에서 API요청시 어떤 도구들을 쓸 수 있는지 명시
    - 각 도구는 이름, 설명, 파라미터 스키마등으로 정의
        - ex: get_weather(city: String)

1. 프롬프트 기반 질문 -> 모델의 판단
    - LLM사용자가 "서울 날씨 어때?"를 물으면 모델은 자체적으로 판단
        1. 본인이 학습한 지식만으로 판단 불가능한 내용이라고 판단
        2. 본인이 들고 있는 도구 목록 중 get_weather() 도구가 필요하다고 판단

2. 도구 호출 요청 생성
    - 모델이 답변 이전에 먼저 도구 호출 요청을 출력
        ```
        {
            "name": "get_weather", 
            "input": {
                "city": "Seoul"
            },
        }
        ```
    - 도구 실행이 아닌 도구 실행 요청을 보낸거임

3. 실제 도구 실행은 도구 목록을 전달한 클라이언트에서 진행
    - 어플리케이션이 요청을 받아 해당 어플리케이션 코드베이스 기반으로 실제 날씨 API를 호출.
    - LLM은 이 과정에 관여하지 않음

4. 어플리케이션에서 결과 반환
    - "서울, 40도 무더위 날씨"
    - 실행 결과를 tool_result에 담아 LLM에 전달
    ![alt text](<resource/스크린샷 2026-07-07 오후 3.57.08.png>)

5. LLM이 자체적으로 최종 답변 생성
    - 어플리케이션이 반환한 실행 결과를 보고 LLM이 자연어 기반으로 답변 생성



한마디로, LLM은 어떤 로직을 수행하거나 실행하는 주체가 아니다.

그저 자신이 학습한 내용을 바탕으로 추론하고 자기회귀하면서 답변을 생성해낼 뿐이고, 답변을 생성하는데 있어 학습된 내용이 부족하다는 판단이 들면, 혹은 더 적합한 도구가 있다면 tool_use를 사용해 연결된 어플리케이션들이 반환한 결과를 기반으로 답변을 생성해낸다.

## 3. stop_reason

- LLM 모델이 응답을 끝낼때 왜 멈추었는지를 보통 같이 알려주는데, 도구를 호출하고 싶을때는 stop_reason값으로 tool_use가 온다. 한마디로 도구 실행 요청을 위해 응답을 멈춘다는 뜻
- LLM 어플리케이션은 stop_reason 값을 보고 최종 답변인지, 아니면 도구를 실행해달라는 요청인지 판단해서 흐름을 분기시킨다.
    - 위 tool_use 실행 흐름 2 -> 3단계 사이에서 발생함 
    - 도구 호출 요청 생성 -> stop_reason 값으로 tool_use -> 실제 도구 실행은 도구 목록을 전달한 클라이언트에서 진행


## 4. 궁금증

### 1. 왜 이런 구조로 설계 되었는가?
    - 보안과 안정성때문이 크다고 함

    - LLM에게 직접 웹 서칭이나 코드 실행권한을 주게 되면 보안적인 측면에서 위험할 수 있고, LLM 자체의 안정성에서도 리스크가 생긴다고 함

    - LLM이 직접 웹 접근이나 코드 실행권한을 가지고 있지 않고 오직 적합한 도구를 찾아 실행 '요청'할 수 있는 권한만 가지고 있는다면 클라이언트 쪽에서 어떤 도구를 LLM에게 허용할지, 실행전에 어떤 검증을 할지를 완전히 통제 가능하다.

### 2. LLM이 도구를 써야한다고 어떻게 판단하는가?
    - 별도 플러그인으로 판단하는건 아니고, 모델 자체 학습 과정에서 도구 목록이 주어지면 적절한 시점에 올바른 형식의 도구 실행 호출을 생성하는 능력을 학습한 것임

    - 그래서 이런 tool_use 기능 (혹은 function calling 기능)을 지원하는 모델과 지원하지 않모 모델로 나뉜다 함

### 3. 도구 호출 방식?
    - 복잡한 질문일수록 모델이 도구를 여러번에 걸쳐서 호출하게 됨.

    - 검색 도구 호출 -> 결과 보고 -> 결과에 따라 다른 검색 도구 호출 -> 결과 보고 -> 결과에 따라 다른 도구 호출 -> 결과 보고 -> 최종 답변

    - 이런식으로 스스로 판단 해 여러 단계를 반복하는걸 agent loop라고 부르고, 이 기술을 기반으로 tool_use가 동작함

    - 이떄 LLM의 autogressive 특성과 state less 특성으로 인해 도구 호출할때마다 이전 대화 전체(사용자 질문, 모델의 도구 호출 요청, 도구 결과등)을 계속 다시 보냄. agent loop를 돌때마다 대화가 점점 비례해서 길어지게 되는 이유.

### 4. tool_use <-> MCP 차이점
    - tool_use는 LLM이 출력에 필요한 정보 혹은 결과를 얻어내기 위해 적절한 도구를 실행 요청하는 기술

    - MCP는 도구 자체를 정의하고 연결하는 방식을 표준화한 프로토콜

    - 둘은 결쟁관계가 아니라 레이어 자체가 다름. MCP로 도구를 연결하든, 개발자가 직접 도구를 정의하든 최종적으로 모델이 그 도구 실행 요청을 보내는 행위는 똑같이 tool_use를 사용해서 함.

### 5. 사용자 질문에 도구가 한번 쓰이면 모델 API는 두번 호출
1. LLM Application -> LLM Model
    ``` 
    LLM Application에서 사용자 질문, 도구 목록 보냄

    LLM Model에서 결과 도출에 필요한 도구를 담아서 응답 
        - stop_reason: tool_use (JSON Schema) -> API 호출 1회
    ```

2. 도구 실행 - LLM Application 내부 혹은 MCP 기반으로 처리

3. LLM Application -> LLM Model (응답)
    ```
    LLM Application에서 도구 실행 결과와 이전 모든 대화들을 tool_result에 담아서 보냄

    LLM Model에서 최종 답변 혹은 도구 호출이 더 필요하면 새로운 tool_use를 tool_result에 담아서 보냄
        - stop_reason: end_turn → 최종 답변 (loop 종료)  -> API 호출 2회
        - stop_reason: tool_use → 도구 더 필요 (2번으로 돌아가 loop 반복) -> API 호출 2회 + @
    ```

4. 이전 대화를 Model에 보내는 방식

    - 이전 대화를 Message 배열에 담아서 보낼때 이전 대화 전체를 다시 보낸다고 했는데, 이전 대화를 마구잡이로 담아서 보내는게 아니다
        1. user       : "서울 날씨 어때?"          (사용자 질문)
        2. assistant  : tool_use (get_weather)     (모델의 도구 호출 요청)
        3. user       : tool_result ("28도, 맑음") (도구 실행 결과)
        4. assistant  : tool_use (***)     (모델의 다른 도구 호출 요청)
        ---
        1. user       : "서울 날씨 어때?"          (사용자 질문)
        2. assistant  : tool_use (get_weather)     (모델의 도구 호출 요청)
        3. user       : tool_result ("28도, 맑음") (도구 실행 결과)
        4. assistant  : tool_use (***)     (모델의 다른 도구 호출 요청)
        5. user       : tool_result ("대충 다른 대답") (도구 실행 결과)
        6. assistant  : "서울은 28도로 맑아요"      (모델의 최종 답변)

        이런식으로 매번 보낼때마다 이런식으로 히스토리가 누적되서 보내게 된다.

    - 히스토리 누적 순서에서 2,4번 tool_use와 3,5번 tool_result는 항상 짝을 이뤄야함
        - Model이 이 도구 써줘라고 하면 그에 대한 결과가 나와야하기 때문

5. role 지정
    - 위의 user, assistant가 role임
    - 대략 누가 한 말인지 role을 반드시 표시 해줘야함
        - user: 사용자 질문 & tool_result 및 도구 실행 결과
        - assistant: 모델의 발화. tool_use 및 최종 답변
    - 도구 결과(tool_result)는 사람이 입력한 게 아닌데도 role이 user여서 이건 그냥 외워야함