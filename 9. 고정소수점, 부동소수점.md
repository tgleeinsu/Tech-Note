# 고정소수점, 부동소수점
- 고정 소수점
    - 정수 표현법을 실수에 적용하는 간단한 방법
    - n자리 비트 패턴이 있을때 언제나 정해진 자리에 소수점이 찍힌다는 의미
        - 8비트가 있으면 앞의 8비트를 소수점자리 이상, 뒤의 3비트를 소수점자리 이하로 하자는 약속
    - 최상위 비트는 부호로 사용
        - 7비트로 실수 표현 가능.
        - 상위 4비트는 정수, 하위 3비트는 소수 표현
            - 기호로는 Q4.3
            - 00100111 -> 100.111을 의미
            - 11011101 -> -1011.101을 의미
    - 가끔 고정소수점 사용하는 라이브러리 보면
        - 기본 정수형 크기 32비트인데 이때 Q15.16등의 포맷을 사용
    - 장점
        - 표현할 수 있는 범위의 값은 확실히 표현 가능. 오차가 없어야하는 제품에 적합 (은행)
        - 예전에는 CPU에서 실수 계산을 해주는 전담장치가 없었기에 이런 걸 직접 구현해서 쓰는 경우가 많았음
    - 단점
        - 표현 가능한 수의 범위가 작아짐
        - 그렇다고 정수 부분 늘리면 소수점 정밀히 표현 불가
        - CPU에 달린 실수 계산 전담장치는 고정 소수점 수 지원 안함. 속도문제
        - 언어에서도 자체적으로 지원하는 경우 드물어짐
- 부동 소수점
    - 오차가 있음. 어떤 데이터를 넣느냐에 따라 오차가 커질수도, 혹은 없을수도 있음
    - 소수점이 둥둥 뜨다닌단 뜻. floating
        - 개념상으로 1234.56이 있다면 소수점을 오른쪽으로 1자리 옮기면 12345.6
        - 오른쪽으로 8자리 옮기면 123456000000
        - 왼쪽으로 8자리 옮기면 0.0000123456
    - 실제 유효한 숫자는 같지만 소수점을 움직이는 것만으로도 표현 가능한 범위가 늘어남. -> 고정소수점 범위 문제 해결
    - 정밀도, 오차등을 희생해서...

    - 장점
        - 소수점을 인위적으로 옮길 수 있으니 표현 가능한 범위 넓음
        - 근대 CPU들어서 다 지원, 웬만한 프로그래밍 언어에서 Float쓰면 IEEE 754 표준 다 지원
            - float, single(32bit)
            - double(64bit)
    - 단점
        - 돈관련 프로그램에 쓰기 부적합
            - 데이터 저장하고 다시 꺼내올 때 1001240.15f -> 1001240.125f -> 1001240.13f 이런식으로 완전한 정밀함을 기대하기 어려움
            - 그러나 대부분의 경우에는 적합
                - 게임에서 총 끝에서 나는 연기 효과를 보여준다든지..
    - 컴퓨터에서 가수와 지수만으로 부동 소수점을 어떻게 표현하는지?
        - 32비트 부동 소수점 IEEE 754. float
            - 부호비트: 1비트. 0이면 양수, 1이면 음수
            - 지수(n)비트: 8비트. 음수 양수 표현 가능
                - 실제 지수는 양수 음수 모두 표현 가능이기 때문에 항상 -127를 해야함. 
                    - 지수비트가 0이라면 0 - 127 -> 2^-127
                    - 127이라면 127 - 127 -> 2^0
                    - 255였다면 255 - 127 -> 2^128
                - 지수비트가 1001 1100 이면 실제 지수는 156 - 127 -> 2^29
            - 가수(m)비트: 23비트.
        - 컴퓨터에서는 부동소수점을 언제나 정규화한 과학적 표기법 사용해 사용.
            - 가수의 정수부는 언제나 1
                - 10011001010.001 -> 1.0011001010001 * 2^11
                - 부동소수점 적어놓은거를 2진수로 바꿀때는 +- 1 * (1.bbbbbb...) * 2^e
            - 예
                - 110.1101011을 정규화
                    - (+1) * 1.101101011 * 2^2
                - 0.00101을 정규화
                    - (-1) * 1.01 * 2^-3