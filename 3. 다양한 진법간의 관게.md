# 다양한 진법간의 관계

- 컴퓨터가 데이터를 표현하는 방법과 2,8,10,16 여러가지 진법과 어떤 관계가 있는가?
    - 2진수 중요함. 비트는 0 혹은 1을 저장함. 이러한 비트가 8개 모이면 바이트
    - 8진수 (2의 3승) 16진수 (2의 4승)도 중요한가?
        - 16진수 중요함. 16진수 한자리에 올 수 있는 경우의 수는 16개. 16개 값을 표현하려면 몇 비트가 필요한가? -> 비트가 1개일땐 2개의 값(0,1) 2개일때는 4개의 값, 비트가 하나 증가할때마다 값이 두배씩 증가. 즉, 16개 값 표현하려면 비트 4개 필요
            - 비트가 n개일때는 몇개의 값 표현 가능? 2의 n승 개의 값 표현 가능.   
            - 4개의 비트가 하나의 16진수 표현. 0x9 -> 1001, 0xD -> 1101
            - 2개의 16진수 표현하려면? 0111 1110 -> 0x7E -> 8개의 비트가 필요하니 1개의 바이트와 같다 -> 1B는 16진수 2개로 표현 가능
            - 0b01111110 보다 0x7E가 읽기 더 편함.
            - #FFFFFF -> 색상값. 모두 16진수. 0x대신 #을 사용할 뿐 16진수 표기는 같음
            - FF, FF, FF RGB값 각각 값인데, 한색의 채도를 0~255로 표현. 256 -> 2의 8승 8비트로 표현
            - 고대 게임에서 스택 에디트 보면 HEX 에디트 열면 00 C0 AC등 16진수로 적혀있음.
- 어쨋든간에 이 모든걸 2진수로 쓰면 보기 어려움. 
    - 홈페이지 메인컬러 보는데 11011010101001101010100 -> #FF08D9
    - 즉 사람들의 가독성 때문에 2진수보다 16진수 선호함 
- 8진수 한자리를 나타내려면 3개 비트 필요함. 000 001 010 011...
    - 메모리, 저장매체 데이터 접근할때 사용하는 데이터 단위는 1바이트 (8비트)이므로 8진수는 데이터 접근에 적합하지 않음.
        - 메모리에서 00101111 1byte읽으려 했더니 8진수 기준으로는 001 011 11?로 하나 끊김.
    - 반면에 16진수는 데이터 접근법 기준으로 매우 훌륭 0010 1111
- 16진수 외 base32 32진법, base64 64진법도 있다 -> 한 자리에 5개 비트, 6개비트 표현
    - 필요한 비트수는? 2의 5승 5개, 2의 6승 6개
    - 모두 메모리 접근에는 유용하지 않음. 8진법과 같은 이유로.
    - 근데 요즘에 은근히 많이 쓰임
        - URL에 허용되지 않은 문자를 URL에 포함해야할때 ?filehash=***/*** 같이 /가 파라미터 뒤에 올때. 파라미터 값을 표현할때
        - PNG를 HTML파일속에 포함(embed)할때도 쓰임.