# 컴퓨터의 정수/문자 표현법

- 글자, 색상등도 내부적으로는 결국 숫자로 저장되어있다.
- integer 정수
    - 양의 정수, 음의정수, 0
    - unsigned 부호 없는, 음의정수는 표현 불가
    - signed 부호 있는, 양의 정수, 0, 음의 정수 모두 표현 가느 
    - 부호 존재 여부에 따라 자료형이 표현가능한 수의 범위가 달라짐
- unsigned integer
    - 0,1,2,3,4...
    - -1나 -123같은 음의 정수 표현 불가
    - 2진수로 표현 쉬움
    - 0, 1,10,11,100...
- 비트 수
    - 비트를 몇개 사용하냐에 따라 표현 가능한 수의 갯수가 결정
    - 비트를 8개 쓰면 몇개의 수를 표현할 수 있나? -> 2의 8승. 256
- 컴퓨터 덧셈시 발생 할 수 있는 문제
    - 1111 1111 + 0000 0001 = 1 0000 0000
    - 이게 왜 문제인가?.
        - 예를들어 uiunt8_t는 부호없는 8비트짜리 정수. 받아올림된 1이 갈곳없어짐. 컴퓨터는 자동으로 9비트로 늘려주지 않음.
        - 최대값은 1111 1111로 정해놨기 때문에 1 더하면 overflow 발생. 그래서 받아올려진 1은 버려짐. 0000 0000됨. 마치 도돌이표처럼
        - 10진수 최대값 99변수에 대입해도 마찬가지 결과 나옴. 어느 진법이든 똑같음
        - 최대값보다 값이 클 경우 최솟값으로 돌아간다는 점.
- signed integer
    - 음수도 표현 가능
        - -1, -2, -3
    - 부호없는 정수에 비해 2진수로 표현하기 어려움.
        - 8비트 안에서 해결하기 위해 가장 앞의 비트가 1이면 음수, 0이면 양수
        - 0011 0100 = 0 -> 부호를 표현, 0110100 -> 숫자를 표현
        - 그러면 나머지 7비트로 숫자를 표현하면 비트는 8비트지만 표현 가능한 정수 범위는 128이 됨
    - 부호비트는 0,1로 똑같이 사용하나 나머지 숫자를 표현하는 7비트는 여러가지 표현방법이 있음
        - 부호 절대값 표기법. signed magnitude representation.
            - 최상위 비트만 사용하려 음수/양수를 구분. 그외 변경사항 없음
            - 0000 0000, 1000 0000
            - 0000 0000 ~ 0111 1111 -> 0 ~ 127
            - 1000 0000 ~ 1111 1111 -> -0 ~ -127
            - 이 방법에는 한계가 잇음. 0이 양수, 음수 두개임. 같은 값인데 비트 패턴이 다름. 또, 양수와 음수를 더할때 비트패턴 둘을 더하는것만으로는 안됨
                - 1 + (-1) = 0000 0001 + 1000 0001 = 1000 0010 = -2
    - 보수
        - 보충을 해주는 수. 숫자의 합이 일정 수가 될 수 있게 도와주는 수
        - 10진법의 보수는? 10의 보수, 9의 보수
            - 10이 되기위해 필요한 수. 2의 10의 보수는 8 / 7의 10의 보수는 3
            - 10의 보수는 10의 n승이 되기 위해 필요한 수. N은 보수를 구하려는 수의 자릿수에 따라 달라짐
            - 3의 9의보수는 6. 483의 9의보수는 516 (999-483). 각 자릿수별 9와 차이로 얻는수.
            - 9의 보ㅓ수는 10의n승 -1이 되기 위해 필요한 수
        - 10의 보수를 구하는 가장 쉬운 방법은 9의 보수를 구한다음에 1을 더하면 됨. 특히 큰 수일수록
            - 5483095497의 10의 보수는?
                - 999999999 - 5483095497 = 4516904502 + 1 = 4516904503
            - 320 - 4
                - 320 - 4 + 1000 - 1000 -> 320 + 996 - 1000 -> 1316 - 1000 -> 316
    - 보수를 이용한 음수 표현
        - 자릿수가 고정이 되어있다면 보수를 이용한 뺄셈이 더 쉽다
            - 320 - 4 -> 00320 - 00004 -> 00320 - 00004 + 100000 - 100000 -> 00320 + 99996 - 100000 -> 00316
            - 오버플로로 마지막 - 100000을 할 필요도 없었음
        - 컴퓨터로 양수 음수 덧셈 혹은 뺄셈을 할때 이런 보수를 사용한 덧셈을 적용.
            - 2의 보수
                - 1100 - 0011 -> 1100 + 0011의 2의 보수 -> 1100 + (10000 - 0011) -> 1100 + 1101 = 1001 으로 설명됨
    - 1100 + (-0011) 음수로 표현하던 것을 1100 + 1101로 보수로 표현이 가능 하다는 것
        - 음수를 보수로 표현 할 수 있다는 것.

    - 2진법의 두 보수
        - 2의 보수(n), 1의 보수 (n-1)
        - 1의 보수는 2의 n승 - 1
        - 1011 + 1011의 1의 보수는 1111