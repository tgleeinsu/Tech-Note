# 비트와 바이트
- 컴퓨터의 데이터를 구성하는 최소 단위
- 왜 2진수만 쓸까?
    - 컴퓨터 구성 단위 중 가장 작은것은 트랜지스터. 흔히말하는 반도체 소자
    - 상태를 두개만 가짐. 전류가 흐르지 않는다. 전류가 흐른다
    - 0 / 1
    - 하나의 트랜지스터 상태(꺼짐과 켜짐)를 기록하는 최소 단위를 비트라고 부름.
    - 단 두개의 숫자만 필요하기 때문에 2진법/2진수를 사용
    - 컴퓨터 데이터는 어떤 것이라도 시작은 비트. 마치 현실세계의 모든 것들도 원소기호를 조합해서 만들 수 있듯이..
- 0,1 외의 값을 표현하고 싶으면? -> 비트를 여러개 쓰면됨. 100 -> 4
    - 5를 표현하고 싶다 -> 101, 3개의 비트 필요
    - 11을 표현하고 싶다 -> 1011, 4개의 비트 필요
- 0번째 비트 bit0는 어느쪽일까? 왼쪽? 오른쪽?
    - 정답은 없음. 진법과 마찬가지로 제일 낮은 비트가 오른쪽, 제일 높은 비트가 왼쪽으로 정하는게 좋을듯
- 비트수와 오디오포맷
    - 오디오 포맷은 WAV, MP3, FLAC등등..
    - 오디오 품질은 비트수에 따라 결정됨. 
    - bitrate
        - 오디오 스트리밍 플랫폼에서 비트레이트 몇 이렇게 볼 수 있음. 특정한 시간단위마다 저장된 비트의 양
        - 256kbit/s -> 1초당 스트리밍으로 들어오는 256 킬로 비트로 받아옴
        - 시간이 동일하다는 전제하에, 담고 있는 데이터의 수가 많을수록 아날로그 처럼 곡선에 가까워진다.
- 컴퓨터에서 데이터를 저장할 용량등을 표현할때는 byte로 표현
    - 비트는 컴퓨터의 가장 기초적인 데이터 형태
    - 바이트는 컴퓨터에 데이터를 저장할때 저장단위
- 바이트 vs 비트
    - 바이트는 비트가 8개 모인 데이터 -> 8b == 1B
    - 왜 바이트를 주로 사용할까? 비트단위는 너무 작고 그나마 사용하기 편한 단위로.. 메모리 또는 저장매체에 보관되어있는 데이터를 읽어올때 (혹은 저장할떄) 최소단위로 바이트 단위로 사용
        - 삼성 SSD 1테라 바이트 등
    - 비트를 쓰는곳도 있음. 인터넷 1초당 1기가 비트
        - 단순히 마케팅의 일환. 1Gb/s는 바이트는 125MB/s 임.
- 기가, 테라 이런건 뭐지?
    - 영어권에서 3자리마다 증가하는 숫자단위 표기법
        - K -> 1,000 -> 1KB
        - M -> 1,000,000 -> 1MB
        - G -> 1,000,000,000 -> 1GB
        - T -> 1,000,000,000,000 -> 1TB
        - P -> 1,000,000,000,000,000 -> 1PB
        - 동아시아권에서는 숫자 4자리마다 단위가 바뀌어서 우리한테는 익숙하지 않음
    - 컴퓨터에서는 1K가 1000이 아니라 1024임.
        - 왜? 1000은 10진수의 산물. 1000은 2진수로 표현하면 1111101000. 1024는 10000000000로 2의 10승 깔끔.
        - 1024 = 2의 10승은 너무 중요.
        - K -> 2의 10승 -> 1KB
        - B -> 2의 20승 -> 1MB
        - G -> 2의 30승 -> 1GB
        - T -> 2의 40승 -> 1TB
        - P -> 2의 50승 -> 1PB
        - 1GB하면 1024MB의미. 근데 하드디스크 제조사, 어댑터 제조사, 인터넷 망 회사는 1GB -> 1000MB로 씀
            - 왜? 상술!